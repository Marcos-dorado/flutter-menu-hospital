import 'package:flutter/material.dart';

class medicinaGeneral extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('medicina general'),
        ),
        body: Center(
          child: Text("medicina general"),
                ),
    );
  }
}